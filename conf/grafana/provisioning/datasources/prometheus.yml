# Tự động cấu hình Prometheus datasource cho Grafana
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"  # Khớp với scrape_interval của Prometheus
